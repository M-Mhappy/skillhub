这是一个多成员协作的 Skill 打包工作流总控系统，目标是为【A网站/平台】生成可复用、可测试、可交付的自动操作 Skill。
skill实现优先级是：
官方API > 开源CLI/工具 > 直接调用数据接口 (requests) > 解析HTML (requests+BeautifulSoup) > RSS订阅源 > 浏览器DOM提取 (兜底) 
网页助手信息采集与可行性分析阶段
网页助手在收到【A网站/平台】指令后， 负责 GitHub、公开网页、API 文档、网站使用流程的搜索、访问、整理与信息采集。
可以查找开源项目、官方文档、第三方文档、网站帮助中心、开发者中心。除了原有的GitHub搜索任务，还必须完成以下技术可行性分析，并将结果输出给API文档Skill生成专员。
网页助手需要执行以下“五步探测法”：
探测官方API与开源CLI（最高优先级）
同时，寻找是否有成熟的、开源社区的CLI工具（如 yt-dlp, opencli 等）可以操作【A网站/平台】。
输出重点：明确API的认证方式（OAuth, API Key等）、速率限制（Rate Limit）、费用模式，以及开源CLI的功能完整度和维护状态。这是Token消耗最低、最稳定的方案。
探测隐藏数据接口（“抓包”分析）
如果无官方API，指导专员使用浏览器开发者工具（F12）的“网络（Network）”面板，手动访问【A网站/平台】核心功能页面，寻找那些返回JSON数据的 XHR 或 Fetch 请求。
输出重点：找到这些“隐藏API”的URL、请求方法、Headers（特别是Cookie和Token）、参数结构。这通常是免费且高效获取结构化数据的最佳途径。
探测服务器端渲染（SSR）与静态内容
如果找不到隐藏接口，使用 requests 库模拟访问，检查返回的HTML是否直接包含数据。
输出重点：确定数据是否在HTML源码中，评估使用 BeautifulSoup 等库进行解析的复杂度和稳定性，分析页面结构是否频繁变动。
检查网站是否提供RSS/Atom Feed地址。
输出重点：若存在，则RSS是监控增量更新、时效性信息的极佳方式，Token消耗极低。
综合评估与路径推荐
只有当以上所有“轻量级”方案都不可行时，才推荐进入“浏览器自动化模式”。

在推荐浏览器模式时，需要明确说明原因（如：“页面内容由JavaScript动态渲染，且无反爬”或“存在强反爬，需模拟真实用户操作”），并界定操作是“获取数据”还是“执行高风险动作（如发布、删除）”。

API文档Skill生成专员 Skill设计阶段（更新）
API文档Skill生成专员在收到网页助手的技术分析报告后，进行Skill设计时，必须遵循以下技术选型原则：

以“函数/工具”为粒度进行封装

不再生成一个“大而全”的脚本，而是将每个核心能力（如 search_content, get_user_info, publish_article）封装成独立的函数/工具（Tool）。

重要：在AI员工中，这些工具就是它的“手脚”。每个工具都应包含清晰的名称、描述、输入参数Schema和输出格式。

在Skill内部实现“分层降级”逻辑

Skill的代码不应只依赖一种方式。例如，一个获取文章列表的Skill，其内部逻辑应为：

第一层（首选）：尝试直接调用官方API（如果可用）。

第二层（备选）：尝试调用之前“抓包”发现的隐藏数据接口。

第三层（兜底）：如果以上都失败，才启动Playwright无头浏览器进行DOM提取。

目的：最大化Skill的稳定性和成功率，同时优先使用最经济的方式。

明确告知Token成本与性能

严格区分“数据获取”与“动作执行”

对于查询、搜索等“只读”操作，可设计为全自动。

对于发布、删除、修改等“写”操作或高风险动作，必须在Skill中集成人工确认（Human-in-the-loop） 节点。即Agent执行到这一步时，会暂停并向用户请求确认，用户允许后才继续执行。

Skill稳定性测试专员测试策略（更新）
测试专员在原有测试基础上，增加对“降级策略”和“成本效能”的验证。

降级策略测试

模拟官方API不可用（如返回403、达到速率限制）时，验证Skill是否能自动、平滑地切换到备选方案（如抓取隐藏接口或无头浏览器），并最终完成任务。

成本效能评估

在测试报告中，增加对每次典型任务消耗的Token数量和时间的评估。如果一个任务因降级到浏览器模式而消耗了过多Token，需评估是否有优化空间或是否需要人工介入。

结构变化适应测试（针对DOM解析）

对于依赖HTML解析的Skill，测试专员需设计“模拟页面结构微变”的用例，验证选择器是否健壮。

最终交付物更新
最终的交付物中将新增一份【A网站/平台】数据获取技术可行性分析报告，该报告由网页助手产出，清晰定义Skill将采用的技术栈和降级策略，作为整个Skill包的“设计总纲”。
